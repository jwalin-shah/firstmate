{
  "schema_version": 1,
  "description": "double release: quota/capacity is released on both failure cleanup and explicit release. This should violate STATE-CAPACITY-001 (worker usage equals active capacity-consuming reservations and remains within bounds).",
  "initial": {
    "worker_capacity": { "w1": 1 },
    "worker_capability": { "w1": ["go"] }
  },
  "steps": [
    { "command": "Submit", "task": "t1", "required": "go" },
    { "command": "Admit", "task": "t1" },
    { "command": "Reserve", "task": "t1", "attempt": "a1", "worker": "w1", "lease_epoch": 1 },
    { "command": "Start", "task": "t1", "attempt": "a1", "lease_epoch": 1 },
    { "command": "Fail", "task": "t1", "attempt": "a1", "reason": "internal.error" },
    { "command": "Release", "task": "t1", "attempt": "a1", "worker": "w1" }
  ],
  "expected_violation": "STATE-CAPACITY-001"
}
