{
  "schema_version": 1,
  "description": "replay prefix twice: replaying the same durable event prefix twice must yield the same abstract state and not re-authorize external effects. The second replay of the dispatch event should be idempotent. This should violate REPLAY-IDEMPOTENCE-001 if effects are re-issued.",
  "initial": {
    "worker_capacity": { "w1": 1 },
    "worker_capability": { "w1": ["go"] }
  },
  "steps": [
    { "command": "Submit", "task": "t1", "required": "go" },
    { "command": "Admit", "task": "t1" },
    { "command": "Reserve", "task": "t1", "attempt": "a1", "worker": "w1", "lease_epoch": 1 },
    { "command": "Dispatch", "task": "t1", "attempt": "a1", "lease_epoch": 1 },
    { "command": "Dispatch", "task": "t1", "attempt": "a1", "lease_epoch": 1 }
  ],
  "expected_violation": "REPLAY-IDEMPOTENCE-001"
}
